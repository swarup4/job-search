# Claude Code — dev tooling, not application code. Not pushed.
.claude/
CLAUDE.md

# Secrets — one .env per service, none committed
.env
.env.*
!.env.example
server/.env
ai/.env
app/web/.env.local
credentials/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Node
node_modules/
.next/
app/extension/dist/

# Generated resumes
*.aux
*.log
*.out

# Template previews are copied out of templates/Template*/ by import_templates.py
# and named after the row's id, so they are per-database, not source.
templates/previews/

.DS_Store
.vscode/settings.json

backup/